#### Catalog

-  This is `duramen's` "inventory" manager that provides information about the database instances, tables, fields, projections, indices, aggregates, relationships, models and all other metadata describing the data stored in each duramen instance.